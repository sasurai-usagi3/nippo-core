- breadcrumb :new_group_report, @group
.panel.panel-default
  .panel-heading
    h1.panel-title
      = @report.reported_at
      | &nbsp;
      = current_user.nickname
  .panel-body
    = render 'form', report: @report
