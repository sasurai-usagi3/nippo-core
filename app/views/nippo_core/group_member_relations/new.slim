- breadcrumb :new_group_group_member_relation, @group
= form_for [@group, @group_member_relations] do |f|
  .form-group
    = f.collection_select :user_id, NippoCore::User.all, :id, :nickname, {}, class: 'form-control'
  button.btn.btn-default
    | Invite
